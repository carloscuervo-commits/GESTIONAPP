# Credenciales y configuración sensible - no subir al repo
# (deben crearse manualmente en el servidor)
backend/config/config.php
backend/config/config_alegra.php
backend/config/config_correo.php

# Herramienta de migración inicial - ya no se necesita
migrar-datos.html

# Fotos y archivos subidos por la app (reportes de visita, etc.)
backend/uploads/*
!backend/uploads/.gitkeep
